
#include "main.h"

/**
 * print_alphabet - the starting point
 *
 * Return: after program is done it return
 */

void print_alphabet(void)
{
	char i;

	for (i = 'a'; i <= 'z'; i++)
	_putchar(i);
	_putchar('\n');
}
